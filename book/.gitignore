*.aux
*.log
*.toc
*.out
*.idx
*.ind
*.ilg
*.pdf
# main.pdf is a build output (rebuild with `make`) -- not version-controlled.
# Ada.pdf is the committed cover asset (from Ada.svg), so the book builds
# without inkscape; keep it tracked.
!Ada.pdf
