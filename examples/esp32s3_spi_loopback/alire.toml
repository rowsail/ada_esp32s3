name = "esp32s3_spi_loopback"
description = "Bare-metal Ada I2C master self-test (internal cross-coupled loopback) on the ESP32-S3 Jorvik runtime"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["embedded", "esp32s3", "i2c", "driver", "example"]

project-files = ["spi_loopback.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig       = { path = "../../crates/xtensa-dynconfig" }
