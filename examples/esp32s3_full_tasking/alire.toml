name = "esp32s3_full_tasking"
description = "ESP32-S3 demo of the full tasking runtime profile (dynamic tasks, hierarchy, rendezvous, protected entries)"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["tasking", "esp32s3", "smp", "baremetal", "rendezvous", "finalization"]

project-files = ["app.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig       = { path = "../../crates/xtensa-dynconfig" }
