/* Auto-generated: every b612 size.  For a demo that shows them all --
   a real app #includes just the sizes it uses, so the rest aren't compiled. */
#include "b612_12.h"
#include "b612_16.h"
#include "b612_24.h"
